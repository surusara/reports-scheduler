{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "reports-scheduler.serviceAccountName" . }}
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "reports-scheduler.labels" . | nindent 4 }}
{{- end }}
